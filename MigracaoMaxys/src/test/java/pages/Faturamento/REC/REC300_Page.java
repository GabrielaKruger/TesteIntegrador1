package pages.Faturamento.REC;

import org.openqa.selenium.WebDriver;
import pages.BasePage;

public class REC300_Page extends BasePage {
    WebDriver driver;

    public REC300_Page(WebDriver driver) {
        super(driver);
        this.driver = driver;
    }
}
