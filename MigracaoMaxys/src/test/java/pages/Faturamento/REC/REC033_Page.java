package pages.Faturamento.REC;

import org.openqa.selenium.WebDriver;
import pages.BasePage;

public class REC033_Page extends BasePage {
    WebDriver driver;

    public REC033_Page(WebDriver driver) {
        super(driver);
        this.driver = driver;
    }
}
